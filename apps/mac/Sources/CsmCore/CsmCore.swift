// The UniFFI-generated `csm_core.swift` is copied into this directory by
// `build-xcframework.sh`. This file intentionally left minimal — SwiftPM
// picks up the generated source as part of the CsmCore target.
//
// Rename-friendly re-exports can live here if the raw generated names ever
// feel too noisy on call sites.
