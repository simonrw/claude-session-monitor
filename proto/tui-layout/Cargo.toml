[package]
name = "tui-layout-proto"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone throwaway prototype - deliberately not part of the workspace (PRO-222)
[workspace]

[dependencies]
ratatui = "0.29"
